<?xml version="1.0" encoding="UTF-8"?>
<launch>

    <arg name='sdf_gazebo' default='true' />
    <arg name='uda' default='false' />
    <!-- send urdf to param server  -->
    <param name="robot_description" command="$(find xacro)/xacro --inorder '$(find my_robot)/urdf/four_wheel.urdf'" unless="$(arg sdf_gazebo)" />
    <param name="robot_description" command="$(find xacro)/xacro --inorder '$(find my_robot)/urdf/my_robot.xacro'" unless="$(arg uda)" />

</launch>